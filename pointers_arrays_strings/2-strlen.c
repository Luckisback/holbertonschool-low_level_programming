#include "main.h"

/**
 * _strlen -  function that returns the length of a string
 * @s: argument (pointer)
 * Return: provid the result of the function
 */

int _strlen(char *s)
{
	long nc;

	nc = 0;

	while (nc[s] != '\0')
		nc++;
	return (nc);
}
