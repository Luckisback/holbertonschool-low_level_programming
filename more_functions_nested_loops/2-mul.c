#include "main.h"

/**
 * mul - a function that multiplies é numbers
 * @a: the first argument
 * @b: the seconde argument
 * Return: sends back the result obtained
 */

int mul(int a, int b)
{
	int r;

	return (r = a * b);
}
